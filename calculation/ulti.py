# import numpy as np

def find_routes(start_end_points):
    """
    find the acceptable routes based on the start and end points location.
    both input and output are numpy array
    :param start_end_points, N by 4 array, each row represents a set of start and end points.
    in each row, the 4 numbers are start_point_longitude, start_point_latitude, end_point_longitude, end_point_latitude
    :return list of routes, N by 4 array, each row represents a route.
    in each row, the 4 numbers are start_point_longitude, start_point_latitude, end_point_longitude, end_point_latitude
    """
    pass